<%= link_to relationship_path(Relationship.find_by(follower_id: current_user.id, followed_id: user.id)), id: "unfollow-#{user.id}", remote: true, method: :delete do  %>
  <i class="bi bi-person-check-fill"></i>
<% end %>